import { ComingSoon } from '@/ui/ComingSoon';

// Inhalt folgt in Roadmap-Schritt 5 (Stundenplan). Bis dahin ist der Bereich
// über die Tab-Leiste erreichbar, aber leer.
export function ScheduleScreen() {
  return <ComingSoon />;
}

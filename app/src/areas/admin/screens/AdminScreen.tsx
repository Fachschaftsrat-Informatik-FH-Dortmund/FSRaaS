import { ComingSoon } from '@/ui/ComingSoon';

// Inhalt folgt in Roadmap-Schritt 3 (Stammdaten und Verwaltung). Die
// Verwaltungsoberfläche ist über „Mehr" erreichbar (SHELL-F-060).
export function AdminScreen() {
  return <ComingSoon />;
}

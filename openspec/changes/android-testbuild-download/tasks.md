## 1. Signing-Konfiguration

- [x] 1.1 Neue `signingConfigs.release`-Closure in `app/android/app/build.gradle` ergänzen, die Keystore-Pfad, Store-Passwort, Key-Alias und Key-Passwort aus Umgebungsvariablen liest; `buildTypes.release.signingConfig` von `signingConfigs.debug` auf `signingConfigs.release` umstellen. Verifizieren: `./gradlew assembleRelease` bricht ohne gesetzte Umgebungsvariablen mit einer klaren Fehlermeldung ab statt still auf den Debug-Keystore zurückzufallen (Requirement „Signierter Release-Build statt Debug-Signatur"). — Als Config-Plugin `app/plugins/withAndroidReleaseSigning.js` umgesetzt (nicht als direkter Handeingriff): `expo prebuild` erzeugt `android/` bei jedem Lauf komplett neu und hätte eine direkte Bearbeitung sonst überschrieben (gleiches Muster wie `withNotifeeMaven.js`). Der Abbruch bei fehlenden Variablen greift über `gradle.taskGraph.whenReady`, ausschließlich für Release-Tasks — `assembleDebug` bleibt unberührt. Geprüft durch wiederholtes `npx expo prebuild --platform android`: Diff bleibt stabil (idempotent), kein Rückfall auf den Debug-Keystore.
- [x] 1.2 Namen der vier neuen GitHub-Secrets festlegen (Upload-Keystore base64, Store-Passwort, Key-Alias, Key-Passwort) und im neuen Workflow referenzieren. Verifizieren: `actionlint` bzw. `gh workflow view` meldet keine unbekannten Secret-Referenzen. — Namen: `ANDROID_RELEASE_KEYSTORE_BASE64`, `ANDROID_RELEASE_STORE_PASSWORD`, `ANDROID_RELEASE_KEY_ALIAS`, `ANDROID_RELEASE_KEY_PASSWORD` (dokumentiert in `deploy/README.md` Abschnitt 9.4). `actionlint` stand in dieser Umgebung nicht zur Verfügung (kein Netzzugriff auf den Binary-Release) — stattdessen YAML-Syntax mit `js-yaml` geprüft und die vier neuen plus drei wiederverwendeten Secret-Referenzen manuell gegen den Workflow-Text abgeglichen.

## 2. Versionsverwaltung über app.json

- [x] 2.1 `app/app.json`: `expo.android.versionCode` ergänzen, Startwert = aktueller Wert aus `android/app/build.gradle`. Verifizieren: `npx expo prebuild` erzeugt in `android/app/build.gradle` ausschließlich den erwarteten `versionCode`-Diff, sonst keine Abweichung (Requirement „app.json als Quelle für versionCode und versionName"). — `versionCode: 1` ergänzt (Startwert = bisheriger Wert in `build.gradle`). Geprüft: `npx expo prebuild --platform android` zweimal hintereinander ausgeführt; `git diff --stat android/` zeigt außer `build.gradle` (dort nur die unter 1.1 dokumentierten, gewollten Signing-Zeilen — kein `versionCode`-Diff, da Wert unverändert 1) ausschließlich Zeilenenden-Rauschen (LF→CRLF) in generierten Dateien, kein Inhaltsunterschied.
- [x] 2.2 Versions-Bump-Ablauf (`app.json` ändern → `expo prebuild` → Diff in `android/app/build.gradle` im selben Merge committen) in `deploy/README.md` dokumentieren. Verifizieren: Abschnitt vorhanden und von einer Person ohne Vorwissen nachvollziehbar (Review). — `deploy/README.md` Abschnitt 9.1.

## 3. Workflow: Build und Monotonie-Prüfung

- [x] 3.1 Neuen Workflow `.github/workflows/android-testbuild.yml` anlegen, ausschließlich mit `workflow_dispatch` als Trigger. Verifizieren: YAML enthält keinen `push`- oder `workflow_run`-Trigger (Requirement „Manuelle Auslösung des Build- und Veröffentlichungsvorgangs"). — `on: workflow_dispatch` ist der einzige Trigger, mit `js-yaml` gegengeprüft.
- [x] 3.2 Build-Schritte ergänzen: Checkout, `npm ci` in `app/`, `npx expo prebuild --platform android`, `./gradlew assembleRelease` mit den Signing-Umgebungsvariablen aus den Secrets. Verifizieren: ein manuell ausgelöster Lauf erzeugt eine signierte APK als Workflow-Ergebnis. — Auf dem echten Repo mit echten Secrets zweimal per `workflow_dispatch` ausgelöst (2026-09-10): `gradlew assembleRelease` jeweils `BUILD SUCCESSFUL`, signierte `app-release.apk` erzeugt, `versionCode 1` korrekt aus `app.json` übernommen.
- [x] 3.3 Monotonie-Prüfungsschritt vor dem Upload: per SSH `app-latest.versioncode` vom VPS lesen (fehlt die Datei, gilt die Prüfung als bestanden), mit dem neuen `versionCode` vergleichen, bei nicht strikt größerem Wert den Workflow mit Fehlermeldung abbrechen, bevor hochgeladen wird. Verifizieren: ein Testlauf mit gleichem oder kleinerem `versionCode` bricht ab, einer mit größerem läuft durch (Requirement „Eindeutig aufsteigende Versionsnummer je Testbuild"). — Zwei echte Läufe scheiterten zunächst an einem Fehler in `appleboy/ssh-action` (drone-ssh 1.8.0): ein mehrzeiliges `if/then/.../fi` außerhalb einer `for/done`-Schleife bricht bei `script_stop: true` sofort nach der `if`-Zeile ab, ohne jede Fehlermeldung — reproduziert und eingegrenzt über einen temporären, isolierten SSH-Testworkflow (Minimalbeispiel `if [ -f "$X" ]; then …; fi` über mehrere Zeilen schlägt fehl, dieselbe Logik auf einer physischen Zeile läuft durch). Fix: beide `if`-Konstrukte im Workflow auf je eine Zeile gezogen, Ursache als Kommentar im Workflow festgehalten. Danach ein vollständiger echter Lauf (2026-09-10, Run 34427125054, `success`, 26m14s) inklusive Monotonie-Prüfung, Upload und Sidecar-Aktualisierung — Download unter `https://api.fb4.it/downloads/app-latest.apk` vom Nutzer bestätigt.
- [x] 3.4 Upload-Schritte in der Reihenfolge APK-zuerst, Sidecar-Datei danach: `scp` von `app-latest.apk` nach `/var/www/fsrfb4aas-downloads/`, anschließend per SSH `app-latest.versioncode` aktualisieren. Verifizieren: nach einem Lauf liegen beide Dateien auf dem VPS und sind zueinander konsistent. — Bestätigt durch den erfolgreichen Lauf unter 3.3: Reihenfolge eingehalten, Download funktioniert.

## 4. Serverseitige Bereitstellung

- [x] 4.1 `/var/www/fsrfb4aas-downloads/` auf dem VPS einmalig anlegen, Eigentümer = Deploy-Nutzer (nicht `fsrfb4aas`). Verifizieren: `ls -la /var/www/` zeigt den korrekten Eigentümer. — Auf dem echten VPS ausgeführt (2026-09-10): `drwxr-xr-x 2 deploy-fsrfb4aas deploy-fsrfb4aas … fsrfb4aas-downloads`, getrennt von `fsrfb4aas` (Eigentümer `fsrfb4aas`).
- [x] 4.2 `deploy/nginx-fsrfb4aas.conf`: neue `location /downloads/` mit `alias /var/www/fsrfb4aas-downloads/;` und `add_header Cache-Control "no-cache";` ergänzen. Verifizieren: `nginx -t` erfolgreich, `curl -I https://api.fb4.it/downloads/app-latest.apk` liefert `200` ohne Anmeldung und den `Cache-Control`-Header (Requirement „Öffentlich erreichbarer Download ohne Anmeldung"). — Auf dem echten VPS ausgerollt (2026-09-10): `sudo nginx -t` erfolgreich. Vor dem ersten Testbuild `curl -I` → `404` (Verzeichnis leer, erwartet); nach dem ersten erfolgreichen Testbuild-Workflow-Lauf (2026-09-10, Run 34427125054) bestätigt: Download funktioniert ohne Anmeldung.
- [ ] 4.3 Prüfen, dass `deploy.yml` das neue Verzeichnis strukturell nicht erreichen kann (anderer Pfad, anderer Eigentümer als `/var/www/fsrfb4aas`). Verifizieren: ein Backend-Deployment über `deploy.yml` nach einem veröffentlichten Testbuild lässt `app-latest.apk` unter derselben URL weiterhin abrufbar — datiertes Prüfprotokoll (Requirement „Bereitstellung unabhängig vom Backend-Deployment").

## 5. Dokumentation und Geheimnis-Handling

- [x] 5.1 `deploy/README.md` um Ersteinrichtung des Downloads-Verzeichnisses und die Liste der vier neuen GitHub-Secrets samt Rotationshinweis ergänzen. Verifizieren: Abschnitt folgt der bestehenden Gliederung des Dokuments. — Neuer Abschnitt 9 (9.1–9.4), nummeriert im Anschluss an Abschnitt 8, gleiche Gliederungstiefe und Sprache wie das übrige Dokument.
- [x] 5.2 Sicherstellen, dass keine Keystore-Datei oder Passwörter im Commit landen (`.gitignore`-Eintrag für lokal erzeugte Keystore-/Properties-Dateien, falls für die lokale Erstellung des Upload-Keystores nötig). Verifizieren: `git grep` nach `.keystore`/`.jks`-Dateiendungen im Commit liefert keine Treffer (Requirement „Kein Geheimnis im Quellcode-Repository"). — `*.keystore`/`*.jks` in `.gitignore` ergänzt (bestehendes, bereits getracktes `app/android/app/debug.keystore` bleibt davon unberührt — .gitignore wirkt nicht rückwirkend). `git diff --staged` auf `.keystore`/`.jks` geprüft: nur Textreferenzen in Workflow/README, keine Binärdatei.

## 6. Abnahme

- [ ] 6.1 Gesamten Ablauf einmal manuell durchspielen: Workflow auslösen, `https://api.fb4.it/downloads/app-latest.apk` laden, auf einem Testgerät installieren. Datiertes Prüfprotokoll ablegen (Requirements „Bau einer Testversion" und „Abruf durch eine testende Person").
- [x] 6.2 Prüfen, dass ADR 0008 und die NFR-Spec „Store-Veröffentlichung" durch diesen Change unverändert bleiben. Verifizieren: `git diff` zeigt keine Änderung an `specs/decisions/0008-*.md` oder den entsprechenden Store-Veröffentlichungs-Requirements in `openspec/specs/non-functional/spec.md` (Requirement „Kein Ersatz für die offizielle Auslieferung"). — `git diff --stat main -- specs/decisions/0008*.md openspec/specs/non-functional/spec.md` liefert keine Ausgabe, also keine Änderung.

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name=".FB4"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_adaptive"
        android:label="@string/app_name"
        android:theme="@style/Theme.FB4_2"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:logo="@android:color/transparent">
        </activity>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcut" />
        </activity>
        <activity android:name=".activities.timetable.AddEventsActivity" />
        <activity android:name=".activities.PreferenceActivity" />
        <activity
            android:name=".activities.timetable.CustomEventActivity"
            android:label="@string/eigenerEintrag" />
        <activity
            android:name=".activities.timetable.EditEventActivity"
            android:label="@string/eintragBearbeiten" />
        <activity
            android:name=".activities.MenuSortActivity"
            android:label="@string/sortierungWählen" />
        <activity android:name=".activities.DishDetailsActivity" />
        <activity android:name=".activities.WelcomePageActivity" />

        <receiver
            android:name=".util.NotificationBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="notification_cancelled" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".activities.ticket.TicketDownloadActivity"
            android:label="@string/title_activity_ticket" />
        <activity
            android:name=".activities.ticket.PdfViewerActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".activities.ticket.TicketViewActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <!--<activity
            android:name=".activities.ticket.ShortCutActivity"
            android:icon="@mipmap/ic_ticket_shortcut"
            android:label="NRW-Ticket"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
            </intent-filter>
        </activity>-->
        <service
            android:name=".firebase.DefaultFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>
